# -*- coding: utf-8 -*-
"""core —— 内核：与主循环强耦合的两样东西。

    protocol.py   输出协议契约 + 默认的文本协议
    loop.py       ReAct 主循环骨架 + 三个钩子

其余接缝（LLM / 工具 / 记忆 / 人格）都是**松耦合**的，各自待在仓库根目录的
自己文件里。改内核的边界是「耦合度」，不是「文件数」。
"""
